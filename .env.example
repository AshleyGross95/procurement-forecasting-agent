# Copy this file to .env to configure local settings.
# Default (MOCK_MODE=true) runs entirely on deterministic synthetic-data logic -- no key needed.

MOCK_MODE=true

# Required only when MOCK_MODE=false, to enable live narration via src/llm.py.
ANTHROPIC_API_KEY=
